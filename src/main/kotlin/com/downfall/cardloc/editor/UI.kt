package com.downfall.cardloc.editor

import java.awt.Color
import javax.swing.BorderFactory

object UI {
    val bg = Color(40, 40, 40)
    val bgAlt = Color(50, 50, 50)
    val panel = Color(45, 45, 45)

    val inputBg = Color(35, 35, 35)
    val previewBg = Color(28, 28, 28)

    val text = Color(220, 220, 220)
    val subtleText = Color(170, 170, 170)

    val border = Color(80, 80, 80)
}